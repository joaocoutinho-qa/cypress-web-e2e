describe ('search product',() => {                       
    beforeEach(() => {
        cy.openWebPage()
    });

    it.only('search for a product that exists', function () { 
     
    })

    it('search for a product that does not exists', function () { 
       
    })

})
